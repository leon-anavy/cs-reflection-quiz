services:
  - type: web
    name: cs-reflection-quiz
    runtime: node
    buildCommand: npm install && npm run build
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: MONGODB_URI
        sync: false  # Set this in the Render dashboard to your MongoDB Atlas connection string
